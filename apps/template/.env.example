VITE_API_URL=
VITE_CLIENT_ID=
VITE_REDIRECT_URI=
VITE_SILENT_REDIRECT_URI=
VITE_POST_LOGOUT_URI=
VITE_SCOPE=
# Login mode (optional): 'redirect' (default — ABP login page, Auth Code + PKCE)
# or 'password' (in-SPA username/password form, ROPC; requires the Password grant
# on the backend client — see docs/BACKEND-KURULUM.md).
VITE_AUTH_MODE=redirect
